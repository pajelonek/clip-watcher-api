spring:
  security:
    basic:
      enabled: false
    enabled: false
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: none

management:
  health:
    db:
      enabled: false
  security:
    enabled: false

security:
  basic:
    enabled: false